# =============================================================================
# 🚀 Project Forge 2025 - Supabase 올인원 템플릿 환경변수
# =============================================================================

# Next.js 기본 설정
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# =============================================================================
# 🎯 Supabase 설정 (필수) - 모든 기능이 여기에!
# =============================================================================
# Supabase 프로젝트 대시보드에서 복사해서 붙여넣기
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# 서버 전용 키 (Admin 작업용, 선택사항)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# 🎉 Supabase에서 제공하는 모든 기능들 (추가 설정 불필요!)
# =============================================================================
# ✅ 인증 (Auth) - 자동 제공
# ✅ 데이터베이스 (PostgreSQL) - 자동 제공  
# ✅ 실시간 (Realtime) - 자동 제공
# ✅ 스토리지 (Storage) - 자동 제공
# ✅ 이메일 (Auth Email) - 자동 제공
# ✅ Edge Functions - 자동 제공
# ✅ 웹훅 - 자동 제공
# ✅ MCP Server - AI 기반 데이터베이스 관리

# =============================================================================
# 🔧 선택적 설정 (고급 사용자용)
# =============================================================================
# 커스텀 이메일 서비스 (Supabase Auth 대신 사용하려면)
# RESEND_API_KEY=your_resend_api_key

# 외부 결제 시스템 (필요시)
# STRIPE_SECRET_KEY=your_stripe_secret_key
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

# 분석 도구 (필요시)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# =============================================================================
# 📖 사용법:
# 1. 이 파일을 복사해서 .env.local로 저장
# 2. Supabase 프로젝트에서 URL과 Key를 복사해서 붙여넣기
# 3. .env.local은 절대 Git에 커밋하지 말것!
# 4. 추가 서비스가 필요한 경우에만 선택적 설정 활성화
# =============================================================================

# 💡 Supabase 설정 가이드:
# 1. https://supabase.com에서 새 프로젝트 생성
# 2. Settings > API에서 URL과 anon key 복사
# 3. 위의 your-supabase-* 부분을 실제 값으로 교체
# 4. Service Role Key는 관리자 기능이 필요할 때만 설정

# 🚀 MCP Server 활용:
# Cursor/Claude에서 자연어로 데이터베이스 작업 가능
# 예: "사용자 테이블을 만들고 기본 RLS 정책을 설정해줘" 