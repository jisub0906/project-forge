# =============================================================================
# 2025년 완벽한 환경 변수 설정 파일 - 스키마 분리 지원
# =============================================================================

# Next.js 설정
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# =============================================================================
# Supabase 설정 (필수) - 스키마 분리 지원
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# 프로젝트별 스키마 분리 설정
# 비용 절감을 위해 하나의 Supabase 인스턴스에서 여러 프로젝트 스키마 사용
NEXT_PUBLIC_SUPABASE_SCHEMA=public
# 예시: project_a, project_b, tenant_123 등 프로젝트별 고유 스키마명

# 개발 환경별 스키마 설정 (선택사항)
# NEXT_PUBLIC_SUPABASE_SCHEMA_DEV=dev_schema
# NEXT_PUBLIC_SUPABASE_SCHEMA_STAGING=staging_schema
# NEXT_PUBLIC_SUPABASE_SCHEMA_PROD=prod_schema

# =============================================================================
# 외부 서비스 연동
# =============================================================================
# 이메일 서비스 (선택사항)
RESEND_API_KEY=your_resend_api_key

# 파일 업로드 (선택사항)
UPLOADTHING_SECRET=your_uploadthing_secret
UPLOADTHING_APP_ID=your_uploadthing_app_id

# 결제 시스템 (선택사항)
STRIPE_SECRET_KEY=your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# =============================================================================
# 분석 및 모니터링
# =============================================================================
# Google Analytics (선택사항)
NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Vercel Analytics (자동 설정됨)
# Sentry (에러 추적, 선택사항)
SENTRY_DSN=your_sentry_dsn

# =============================================================================
# 개발 도구
# =============================================================================
# 데이터베이스 (로컬 개발용)
DATABASE_URL=your_database_url

# Redis (캐싱, 선택사항)
REDIS_URL=your_redis_url

# =============================================================================
# 보안 설정
# =============================================================================
# NextAuth.js 비밀키 (인증 시스템 사용 시)
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# JWT 시크릿 (추가 보안이 필요한 경우)
JWT_SECRET=your_jwt_secret

# 웹훅 검증용 시크릿
WEBHOOK_SECRET=your_webhook_secret

# =============================================================================
# 사용법:
# 1. 이 파일을 복사해서 .env.local로 저장
# 2. 필요한 값들을 실제 API 키로 교체
# 3. .env.local은 절대 Git에 커밋하지 말것!
# ============================================================================= 